# Bulk data — kept locally, not in repo
/images
/images-webp
/data
/logs

# Local-only
.env
.env.local
*.log

# Node / web build
node_modules
dist
dist-ssr
*.local
npm-debug.log*

# public/songs.bin IS committed — it's the XOR+gzip-obfuscated dataset
# Pages serves to the client (decoded in src/lib/songsCodec.ts).

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python
__pycache__/
*.pyc
